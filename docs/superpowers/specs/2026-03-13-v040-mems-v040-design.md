# Friedman-cli v0.4.0 Design Spec — Wrapping MEMs v0.4.0

## Overview

Friedman-cli v0.4.0 wraps MacroEconometricModels.jl v0.4.0, adding 6 feature areas:
DSGE historical decomposition, panel regression, spectral analysis,
ordered/multinomial choice models, data enhancements, and dependency updates.

Total: 14 top-level commands (+1: `spectral`), ~197 subcommands (+33 from ~164).

## 1. Version & Dependency Changes

| Item | Old | New |
|------|-----|-----|
| Friedman-cli version | 0.3.5 | 0.4.0 |
| MEMs compat | 0.3.5 | 0.4.0 |
| FFTW | weak dep | regular dep |
| NonlinearSolve | — | new regular dep (v4) |
| PATHSolver UUID | `f5f7c340-0bb3-4c5b-...` | `f5f7c340-0bb3-5c69-969a-41884d311d1b` |

Files to update:
- `Project.toml` — version, MEMs compat, FFTW → deps, add NonlinearSolve, fix PATHSolver UUID
- `src/Friedman.jl` — `FRIEDMAN_VERSION`
- `src/cli/types.jl` — `Entry` default version
- `test/runtests.jl` — version string references

## 2. DSGE Historical Decomposition

### Commands

**`dsge hd`** — Linear DSGE historical decomposition via Kalman smoother.

```
friedman dsge hd <model> --data <csv> --observables <var1,var2,...> \
    [--states=observables|all] [--measurement-error=<csv|auto>] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_dsge_hd(; model, data, observables, states, measurement_error, ...)`

Flow:
1. `_load_dsge_model(model)` → DSGESpec
2. `_solve_dsge(spec)` → DSGESolution
3. `load_data(data)` → DataFrame → `df_to_matrix()` → Matrix
4. Parse `observables` string → `Vector{Symbol}`
5. `historical_decomposition(sol, Y, obs; states, measurement_error)` → HistoricalDecomposition
6. `verify_decomposition(hd)` → print diagnostics
7. Build contributions table per shock → `output_result()`
8. `_maybe_plot()` if --plot/--plot-save

**`dsge bayes hd`** — Bayesian DSGE HD with posterior uncertainty.

```
friedman dsge bayes hd <model> --data <csv> --params <toml> --priors <toml> \
    --observables <var1,var2,...> \
    [--method=smc|rwmh] [--draws=5000] [--mode-only] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_dsge_bayes_hd(; model, data, params, priors, observables, method, draws, mode_only, ...)`

Flow:
1. Load model, estimate posterior via `estimate_dsge_bayes()`
2. `historical_decomposition(bd, Y, obs; mode_only)` → BayesianHistoricalDecomposition
3. Output mean contributions + credible intervals

### MEMs API Used

```julia
historical_decomposition(sol::DSGESolution, data, observables; states, measurement_error)
historical_decomposition(bd::BayesianDSGE, data, observables; mode_only)
contribution(hd, var, shock)      # accessor
verify_decomposition(hd)          # diagnostic
dsge_smoother(sol, data, obs)     # KalmanSmootherResult (internal)
```

### New Types

- `HistoricalDecomposition{T}` — contributions (T_obs x n_vars x n_shocks), initial_conditions, actual, shocks, method
- `BayesianHistoricalDecomposition{T}` — mean contributions + quantiles
- `KalmanSmootherResult` — smoothed states, shocks, loglikelihood

## 3. Panel Regression

### Estimation Commands (under `estimate`)

**`estimate preg`** — Panel linear regression (FE/RE/FD/Between/CRE).

```
friedman estimate preg <data> --dep <col> \
    [--method=fe|re|fd|between|cre] [--twoway] \
    [--cov-type=ols|robust|cluster|driscoll-kraay] \
    [--id-col=<col>] [--time-col=<col>] \
    [-o output] [-f format]
```

Handler: `_estimate_preg(; data, dep, method, twoway, cov_type, id_col, time_col, ...)`

Flow:
1. `load_data(data)` → DataFrame
2. `xtset(df, id_col, time_col)` → PanelData
3. Split dep/X from DataFrame columns
4. `estimate_xtreg(panel, dep, X; method, twoway, cov_type)` → PanelRegModel
5. Output coefficient table (beta, SE, t, p, CI) + R2 within/between/overall + F-stat + sigma_u/sigma_e/rho

**`estimate piv`** — Panel IV/2SLS.

```
friedman estimate piv <data> --dep <col> --endogenous <cols> --instruments <cols> \
    [--method=fe|re|fd] [--cov-type=ols|robust|cluster] \
    [--id-col=<col>] [--time-col=<col>] \
    [-o output] [-f format]
```

Handler: `_estimate_piv(; data, dep, endogenous, instruments, method, cov_type, id_col, time_col, ...)`

**`estimate plogit`** — Panel logit.

```
friedman estimate plogit <data> --dep <col> \
    [--method=fe|re] [--cov-type=ols|robust|cluster] \
    [--id-col=<col>] [--time-col=<col>] \
    [-o output] [-f format]
```

Handler: `_estimate_plogit(; data, dep, method, cov_type, id_col, time_col, ...)`

**`estimate pprobit`** — Panel probit.

Same interface as `estimate plogit` but calls `estimate_xtprobit`.

### Predict / Residuals (under `predict` and `residuals`)

Add leaves: `predict preg`, `predict piv`, `predict plogit`, `predict pprobit`
and `residuals preg`, `residuals piv`, `residuals plogit`, `residuals pprobit`.

All follow the same pattern: load data → xtset → estimate model → `predict(model)` / `residuals(model)`.

### Panel Specification Tests (under `test`)

| Command | MEMs Function | Description |
|---------|--------------|-------------|
| `test hausman` | `hausman_test(fe, re)` | FE vs RE specification |
| `test breusch-pagan` | `breusch_pagan_test(model)` | RE LM test |
| `test f-fe` | `f_test_fe(model)` | F-test for fixed effects |
| `test pesaran-cd` | `pesaran_cd_test(model)` | Cross-sectional dependence |
| `test wooldridge-ar` | `wooldridge_ar_test(model)` | Panel serial correlation |
| `test modified-wald` | `modified_wald_test(model)` | Groupwise heteroskedasticity |

Panel tests require estimating a model first, so each test command accepts
`<data> --dep --id-col --time-col` and estimates internally.

`test hausman` is special: it estimates both FE and RE models, then compares.

### Shared Helpers (shared.jl)

```julia
_PREG_COMMON_OPTIONS  # [dep, id-col, time-col, cov-type, method, twoway, output, format]
_load_panel_for_preg(data, id_col, time_col, dep)  # → (panel, y, X, varnames)
_preg_coef_table(model)  # → DataFrame with beta, SE, t, p, CI columns
```

### MEMs API Used

```julia
estimate_xtreg(panel, y, X; method, twoway, cov_type) → PanelRegModel
estimate_xtiv(panel, y, X, Z; endogenous, method, cov_type) → PanelIVModel
estimate_xtlogit(panel, y, X; method, cov_type) → PanelLogitModel
estimate_xtprobit(panel, y, X; method, cov_type) → PanelProbitModel
hausman_test(fe, re) → PanelTestResult
breusch_pagan_test(model) → PanelTestResult
f_test_fe(model) → PanelTestResult
pesaran_cd_test(model) → PanelTestResult
wooldridge_ar_test(model) → PanelTestResult
modified_wald_test(model) → PanelTestResult
```

## 4. Spectral Analysis (New Top-Level Command)

### Commands

New file: `src/commands/spectral.jl` with `register_spectral_commands!()`.

**`spectral acf`** — Autocorrelation and partial autocorrelation.

```
friedman spectral acf <data> \
    [--column=1] [--max-lag=<int>] [--ccf-with=<col>] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_spectral_acf(; data, column, max_lag, ccf_with, ...)`

Flow:
1. `load_data(data)` → extract column vector
2. `acf(y; maxlag)` → ACFResult (includes ACF, PACF, Q-stats, p-values)
3. If `--ccf-with`, also call `ccf(y, z)`
4. Output table: lag | ACF | PACF | Q-stat | p-value
5. `_maybe_plot()`

**`spectral periodogram`** — Raw periodogram.

```
friedman spectral periodogram <data> [--column=1] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_spectral_periodogram(; data, column, ...)`

**`spectral density`** — Spectral density estimation.

```
friedman spectral density <data> [--column=1] \
    [--method=periodogram|welch|smoothed|ar] [--bandwidth=<float>] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_spectral_density(; data, column, method, bandwidth, ...)`

**`spectral cross`** — Cross-spectral analysis.

```
friedman spectral cross <data> --var1=<col> --var2=<col> \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_spectral_cross(; data, var1, var2, ...)`

Output: freq | co-spectrum | quad-spectrum | coherence | phase | gain

**`spectral transfer`** — Filter transfer function.

```
friedman spectral transfer [--filter=hp|bk|hamilton|ideal] \
    [--lambda=<float>] [--nobs=<int>] \
    [--plot] [--plot-save=<path>] [-o output] [-f format]
```

Handler: `_spectral_transfer(; filter, lambda, nobs, ...)`

Note: No data file needed — computes theoretical transfer function.

### Spectral Diagnostic Tests (under `test`)

| Command | MEMs Function | Description |
|---------|--------------|-------------|
| `test fisher` | `fisher_test(y)` | Periodicity (hidden periodicities) |
| `test bartlett-wn` | `bartlett_white_noise_test(y)` | Cumulative periodogram white noise |
| `test box-pierce` | `box_pierce_test(y; lags)` | Portmanteau (Box-Pierce) |
| `test durbin-watson` | `durbin_watson_test(y)` | First-order autocorrelation |

### MEMs API Used

```julia
acf(y; maxlag) → ACFResult
pacf(y; maxlag) → ACFResult (uses same type)
ccf(y, z; maxlag) → ACFResult
periodogram(y) → SpectralDensityResult
spectral_density(y; method, bandwidth) → SpectralDensityResult
cross_spectrum(y, z) → CrossSpectrumResult
transfer_function(filter; lambda, nobs) → TransferFunctionResult
fisher_test(y) → FisherTestResult
bartlett_white_noise_test(y) → BartlettWhiteNoiseResult
box_pierce_test(y; lags) → BoxPierceResult
durbin_watson_test(y) → DurbinWatsonResult
```

## 5. Ordered & Multinomial Choice Models

### Estimation Commands (under `estimate`)

**`estimate ologit`** — Ordered logistic regression.

```
friedman estimate ologit <data> --dep <col> \
    [--cov-type=hc0|hc1|hc2|hc3|cluster] [--clusters=<col>] \
    [-o output] [-f format]
```

Handler: `_estimate_ologit(; data, dep, cov_type, clusters, ...)`

Flow:
1. `_load_reg_data(data, dep)` → (y, X, varnames)
2. `estimate_ologit(y, X; cov_type, varnames)` → OrderedLogitModel
3. Output: cutpoints + slope coefficients table (beta, SE, z, p, CI) + pseudo-R2 + AIC/BIC

**`estimate oprobit`** — Ordered probit. Same interface, calls `estimate_oprobit`.

**`estimate mlogit`** — Multinomial logit.

```
friedman estimate mlogit <data> --dep <col> \
    [--base-category=<int>] [--cov-type=hc0|hc1|hc2|hc3] \
    [-o output] [-f format]
```

Handler: `_estimate_mlogit(; data, dep, base_category, cov_type, ...)`

Output: Per-category coefficient table + pseudo-R2 + AIC/BIC.

### Predict / Residuals

- `predict ologit` / `predict oprobit` — predicted probabilities per category
- `predict mlogit` — predicted probabilities per category
- `residuals ologit` / `residuals oprobit` / `residuals mlogit` — deviance residuals

### Tests (under `test`)

| Command | MEMs Function | Description |
|---------|--------------|-------------|
| `test brant` | `brant_test(model)` | Parallel regression (ordered models) |
| `test hausman-iia` | `hausman_iia(model)` | IIA for multinomial logit |

These accept `<data> --dep` and estimate internally before testing.

### MEMs API Used

```julia
estimate_ologit(y, X; cov_type, varnames) → OrderedLogitModel
estimate_oprobit(y, X; cov_type, varnames) → OrderedProbitModel
estimate_mlogit(y, X; cov_type, varnames) → MultinomialLogitModel
brant_test(model) → test result
hausman_iia(model) → test result
marginal_effects(model)  # for ordered/multinomial
```

## 6. Data Enhancements

### New Leaves (under `data`)

**`data dropna`** — Drop rows with missing/NaN values.

```
friedman data dropna <data> [--vars=<col1,col2,...>] [-o output] [-f format]
```

Handler: `_data_dropna(; data, vars, ...)`

Flow:
1. `load_data(data)` → DataFrame → `TimeSeriesData`
2. `dropna(ts; vars)` → cleaned TimeSeriesData
3. Output cleaned data or summary

**`data keeprows`** — Filter rows by condition.

```
friedman data keeprows <data> --condition=<expr> [-o output] [-f format]
```

Handler: `_data_keeprows(; data, condition, ...)`

Flow:
1. `load_data(data)` → DataFrame → `TimeSeriesData`
2. Parse condition string → row indices
3. `keeprows(ts, indices)` → filtered TimeSeriesData
4. Output filtered data

### MEMs API Used

```julia
dropna(ts::TimeSeriesData) → TimeSeriesData
dropna(pd::PanelData) → PanelData
keeprows(ts, indices) → TimeSeriesData
```

## Updated Command Hierarchy

```
friedman
├── repl
├── estimate     var | bvar | lp | arima | gmm | smm | static | dynamic | gdfm |
│                arch | garch | egarch | gjr_garch | sv | fastica | ml | vecm | pvar |
│                favar | sdfm | reg | iv | logit | probit |
│                preg | piv | plogit | pprobit |        ← NEW (4)
│                ologit | oprobit | mlogit               ← NEW (3)
├── test         [existing 29+4] |
│                hausman | breusch-pagan | f-fe |         ← NEW panel spec (3)
│                pesaran-cd | wooldridge-ar | modified-wald |  ← NEW panel spec (3)
│                fisher | bartlett-wn |                   ← NEW spectral (2)
│                box-pierce | durbin-watson |             ← NEW portmanteau (2)
│                brant | hausman-iia                      ← NEW discrete choice (2)
├── irf          var | bvar | lp | vecm | pvar | favar | sdfm
├── fevd         var | bvar | lp | vecm | pvar | favar | sdfm
├── hd           var | bvar | lp | vecm | favar
├── forecast     var | bvar | lp | arima | static | dynamic | gdfm |
│                arch | garch | egarch | gjr_garch | sv | vecm | favar
├── predict      var | bvar | arima | vecm | static | dynamic | gdfm |
│                arch | garch | egarch | gjr_garch | sv | favar | reg | logit | probit |
│                preg | piv | plogit | pprobit |          ← NEW (4)
│                ologit | oprobit | mlogit                 ← NEW (3)
├── residuals    var | bvar | arima | vecm | static | dynamic | gdfm |
│                arch | garch | egarch | gjr_garch | sv | favar | reg | logit | probit |
│                preg | piv | plogit | pprobit |          ← NEW (4)
│                ologit | oprobit | mlogit                 ← NEW (3)
├── filter       hp | hamilton | bn | bk | bhp
├── data         list | load | describe | diagnose | fix | transform | filter | validate | balance |
│                dropna | keeprows                         ← NEW (2)
├── nowcast      dfm | bvar | bridge | news | forecast
├── dsge         solve | irf | fevd | simulate | estimate | perfect-foresight | steady-state |
│                hd |                                      ← NEW (1)
│                bayes (estimate | irf | fevd | simulate | summary | compare | predictive |
│                       hd)                                ← NEW (1)
├── did          estimate | event-study | lp-did |
│                test (bacon | pretrend | negweight | honest)
└── spectral     acf | periodogram | density | cross | transfer   ← NEW top-level (5)
```

**Totals: 14 top-level commands, ~197 subcommands.**

## New/Modified Source Files

| File | Action | Content |
|------|--------|---------|
| `src/commands/spectral.jl` | NEW | `register_spectral_commands!()`, 5 leaves, 5 handlers |
| `src/commands/estimate.jl` | MODIFY | +7 leaves (preg/piv/plogit/pprobit/ologit/oprobit/mlogit), +7 handlers |
| `src/commands/predict.jl` | MODIFY | +7 leaves, +7 handlers |
| `src/commands/residuals.jl` | MODIFY | +7 leaves, +7 handlers |
| `src/commands/test.jl` | MODIFY | +10 leaves, +10 handlers |
| `src/commands/dsge.jl` | MODIFY | +2 leaves (hd, bayes hd), +2 handlers |
| `src/commands/data.jl` | MODIFY | +2 leaves (dropna, keeprows), +2 handlers |
| `src/commands/shared.jl` | MODIFY | +`_PREG_COMMON_OPTIONS`, +`_load_panel_for_preg()`, +`_preg_coef_table()` |
| `src/Friedman.jl` | MODIFY | version bump, `include("commands/spectral.jl")`, register in `build_app()` |
| `Project.toml` | MODIFY | version, MEMs compat, deps changes |
| `test/mocks.jl` | MODIFY | +all new mock types and functions |
| `test/test_commands.jl` | MODIFY | +handler tests for all new commands |
| `test/runtests.jl` | MODIFY | version strings, spectral command structure tests |

## Mock Types Summary (test/mocks.jl additions)

### Panel Regression
- Types: `PanelRegModel{T}`, `PanelIVModel{T}`, `PanelLogitModel{T}`, `PanelProbitModel{T}`, `PanelTestResult{T}`
- Functions: `estimate_xtreg`, `estimate_xtiv`, `estimate_xtlogit`, `estimate_xtprobit`
- Test functions: `hausman_test`, `breusch_pagan_test`, `f_test_fe`, `pesaran_cd_test`, `wooldridge_ar_test`, `modified_wald_test`

### Ordered/Multinomial
- Types: `OrderedLogitModel{T}`, `OrderedProbitModel{T}`, `MultinomialLogitModel{T}`
- Functions: `estimate_ologit`, `estimate_oprobit`, `estimate_mlogit`, `brant_test`, `hausman_iia`

### Spectral
- Types: `ACFResult{T}`, `SpectralDensityResult{T}`, `CrossSpectrumResult{T}`, `TransferFunctionResult{T}`, `FisherTestResult{T}`, `BartlettWhiteNoiseResult{T}`, `BoxPierceResult{T}`, `DurbinWatsonResult{T}`, `LjungBoxResult{T}`
- Functions: `acf`, `pacf`, `ccf`, `periodogram`, `spectral_density`, `cross_spectrum`, `transfer_function`, `fisher_test`, `bartlett_white_noise_test`, `box_pierce_test`, `durbin_watson_test`, `ljung_box_test`

### DSGE HD
- Types: `HistoricalDecomposition{T}` (DSGE version), `BayesianHistoricalDecomposition{T}`, `KalmanSmootherResult`
- Functions: `historical_decomposition` (DSGESolution dispatch), `historical_decomposition` (BayesianDSGE dispatch), `dsge_smoother`, `contribution`, `verify_decomposition`, `total_shock_contribution`

### Data
- Functions: `dropna` (TimeSeriesData/PanelData dispatches), `keeprows`

## Documentation Updates Required

1. `CLAUDE.md` — version, command hierarchy, command details, deps, testing
2. `README.md` — version, commands table, feature list
3. `docs/make.jl` — add spectral page
4. `docs/index.md` — update feature list
5. `docs/commands/overview.md` — add new commands
6. `docs/architecture.md` — update command counts, deps
7. `docs/api.md` — add new types/functions
8. New doc pages: `docs/commands/spectral.md`, `docs/commands/panel-regression.md`, `docs/commands/ordered-multinomial.md`, `docs/commands/dsge-hd.md`
